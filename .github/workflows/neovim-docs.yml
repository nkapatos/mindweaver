name: Neovim Docs

on:
  pull_request:
    branches:
      - main
    paths:
      - 'clients/neoweaver/**'
      - 'README.md'
      - 'docs/**'
      - '.github/workflows/neovim-docs.yml'
  push:
    branches:
      - main
    paths:
      - 'clients/neoweaver/**'
      - 'README.md'
      - 'docs/**'
      - '.github/workflows/neovim-docs.yml'

permissions:
  contents: write

# TODO: Documentation generation not fully implemented yet
# Commenting out until mini.doc and panvimdoc setup is complete
jobs:
  placeholder:
    name: Placeholder Job
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Documentation generation workflow disabled until fully implemented"
