// Package utils provides a small set of reusable utilities among the server modules
package utils

import "time"

// APITimeFormat is the standard format for all API timestamps (RFC3339).
const APITimeFormat = time.RFC3339
