version: '3'

# Load configuration in layers:
# 1. .env.example provides defaults (committed, works out-of-box)
# 2. .env.local overrides for individual developer needs (gitignored, optional)
dotenv:
  - .env.example
  - .env.local

includes:
  mw: ./packages/mindweaver/tasks/tasks.mw.yml
  brain: ./packages/mindweaver/tasks/tasks.brain.yml
  mind: ./packages/mindweaver/tasks/tasks.mind.yml
